package com.dicoding.fish2eat.ui.Recipe

/*import androidx.lifecycle.*
import com.dicoding.fish2eat.application.data.model.RecipeDao
import com.dicoding.fish2eat.application.data.repository.RecipeRepository
import kotlinx.coroutines.launch

class RecipeViewModel(private val dao: RecipeDao) : ViewModel() {
    val recipes: LiveData<List<RecipeItem>> = dao.getAllRecipes()

    fun insertRecipes(recipes: List<RecipeItem>) {
        viewModelScope.launch {
            dao.insertRecipes(recipes)
        }
    }
}


 */